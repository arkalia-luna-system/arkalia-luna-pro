# Fichier temporaire pour nettoyer le workflow orphelin
name: 📚 Documentation Arkalia-LUNA Pro
on:
  workflow_dispatch:
jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Nettoyage en cours..."
